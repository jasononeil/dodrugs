# Visual Studio Code and the Haxe code completion features fall over when a hxml has more than one compiler target.
# This hxml file is pretty much the same as test.hxml, but with only one target so that all features work.
-main Test
-cp src
-cp test
-lib utest
-lib tink_macro
-lib tink_core

-neko bin/test.n
